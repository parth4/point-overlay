# Point

Static All-In overlay demo. Paste the public link; the stretch plays with POINT / CIRCLING / OVERLAP tags.

- `index.html` — self-contained player + overlay
- `cues.json` — timed tags (also inlined in the page)

YouTube embed only. No video file.
